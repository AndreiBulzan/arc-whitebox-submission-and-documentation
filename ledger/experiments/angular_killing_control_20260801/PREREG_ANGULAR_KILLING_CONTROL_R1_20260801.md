# Angular Killing-field control R1 preregistration

Date: 2026-08-01

Evidence scope: offline **component** screen only. No FlopScope row, package,
upload, submission, or remote action is permitted.

## New observable

For a bias-free network `f` restricted to the unit sphere and any fixed
skew-symmetric matrix `Q`, rotational invariance gives

```text
E_sphere[J_f(u) Q u] = 0.
```

This differs from the killed constant-direction spherical-Stein R1 control.
`Q u` is a nonconstant divergence-free Killing field, and the exact tangent
is propagated through all 32 realized ReLU gate patterns.

Four rank-two fields are fixed from target-free weight geometry. They use an
orthonormal frame formed from the two leading left singular vectors of `W0`
and two leading left singular vectors of the normalized end-to-end linear
product after projection away from the first pair. The planes are `(0,1)`,
`(2,3)`, `(0,2)`, and `(1,3)`.

## Frozen fast screen

- Full rows `0,1`; Generated rows `0,1`.
- Complete antipodal production supports at total `K=16,32`.
- Prefixes of `1,2,4` fields.
- The same two target-free opposite-parity coefficient spellings as the
  prior Stein falsifier: point cross-fit and complete-basis cross-fit.
- Predictions and metadata are sealed before targets are opened by the
  separate scorer.
- One `32768`-point IID sphere diagnostic checks the zero-mean identity.

## Promotion gate

One identical `(mode, K, prefix)` must satisfy all of:

```text
Full pooled raw ratio                 <= 0.85
Generated noise-corrected raw ratio  <= 0.85
maximum observed row ratio           <= 1.50 in both families
all values finite; IID identity diagnostic structurally sound
```

Failure kills this exact four-plane/cross-fit spelling. Passing licenses only
a broader target-free capture and a compute study; it does not establish a
candidate or remote score.
