# Verdict: Haar16 random-feature arc and replicate shrink are killed

R9B froze the R6 development winner and an economic 128-probe alternative,
then captured 32 disjoint networks target-free.  The 512-probe estimator
regressed pooled corrected MSE by 16.9%; the 128-probe estimator regressed it
by 2.3%.  Neither family transfer nor row/bootstrap gates passed.

R9C used two independent Haar16 correction vectors to estimate correction
signal and noise, including output-coordinate cross-fitting.  Although the
median target-free shrink (`0.413`) was numerically close to the target-open
optimal amplitude, the resulting corrections still failed: the best pooled
ratio was `1.00971048` and Full was `1.03803150`.

Close the Gaussian random-feature arc spelling at affordable Haar16 query
size, including its replicate-energy amplitude estimator.  Preserve the
distinct direct-mean calibration question: R9B did not test matching the 129
exact basis-level layer-31 mean vectors directly to the Haar query mean.

Evidence labels: **broad statistical** for R9B; **component post-hoc
development** for R9C.  Neither is a physical or Mini100 receipt.
