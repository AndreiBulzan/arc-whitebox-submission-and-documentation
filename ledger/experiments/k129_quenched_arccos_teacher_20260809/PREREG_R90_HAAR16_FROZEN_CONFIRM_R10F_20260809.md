# Preregistration: frozen R90 plus Haar16 confirmation R10F

Freeze the R10E survivor before touching a disjoint bank:

- incumbent: exact statistical R90/q0 prediction (K129 O0, endpoint
  `lambda=0.0075`, live output scale);
- query geometry: replica-1 deterministic Haar sequence from R10D;
- complete queried bases: `16` (8,192 antipodal rows);
- coefficient: `0.0718364281490907`;
- prediction: `q0 + beta * (haar16_final - q0)`.

Capture on 64 rows disjoint from the R9B/R10 development bank:

- Full `692..723`;
- Generated `40..71`.

The capture may read weights only.  Seal and hash q0, Haar final mean, and the
fixed prediction before any target or Generated label-noise array is mapped.

After sealing, report corrected pooled and family MSE ratios, row wins,
bootstrap uncertainty, and a projection using R90 `137.8297337635B` plus
`17.28B` for 16 complete queried bases.  The candidate passes to physical
implementation only if:

- pooled raw ratio is at most `0.89`;
- both family ratios are below `1`;
- at least 35 of 64 rows improve; and
- the projected adjusted score is below the R90 reference
  `1.1307520123575174e-7`.

This is broad statistical confirmation plus projection.  No physical row,
Mini100 row, package, upload, submission, or remote action is authorized.
