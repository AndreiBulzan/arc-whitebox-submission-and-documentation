# K129 R56 mixed48 direction-240 — R58 preregistration

Date: 2026-08-03

The uniform 240-direction rule was previously tested on the older mixed64,
ridge-1 statistic.  R51 tested a 128-direction downgrade on 16 of R36's
atoms.  No recorded result maps the already sealed 240-direction proxy onto
R56's different 48-atom support and ridge-0.25 fit.

Keep R56's 48 `(frame, basis)` atoms and its 64-state q0 anchor.  Replace only
each alternate atom's 256-direction proxy with the pre-existing fixed first
240-direction capture, and refit the 48 coefficients to the target-free
four-frame teacher at the already selected relative ridge `0.25`.  Seal the
two complete-family predictions before reading scored targets.

The direction count, schedule, support, ridge, and anchor are fixed.  Promote
only if the projected accuracy-compute product improves both Full100 and
Generated128 versus R56.  Evidence is **broad statistical** for sealed raw
accuracy and **projection** for compute/adjusted score.  No physical row,
package, upload, or submission is authorized.

