# K129 R76 — R75 with H1-only DPS48

Date: 2026-08-04

Evidence sought: **measured whole** for one initialized and one steady
target-free current-meter row; **component** for the exact contraction
identity. No broad, upload, submission, or remote action is authorized.

## Prior-art boundary

The capsule search covered `R27 DPS48`, `DPS48 only`, `H1-only`, `transplant`,
and the R27/R30/R31/R66 call paths. R30 proves the standard-basis
`DPS48-accurate/B7/B7` H1 identity, but combines it with R28's wall-slow late
rewrite. R66 explicitly bypasses the entire R28/R30/R31 stack after the
remote timeout. No artifact applies only the proved H1 identity to the
wall-safe R75 execution policy.

## Change and gate

Replace only R27's repaired-H1 segmented product with R30's hash-pinned
standard-basis DPS48 kernel. Preserve R75's cold/steady routing and every
late contraction. The v0.9 component saving was `559,841,280` counted FLOPs;
v0.10 is measured rather than assumed.

Promote only if:

1. bare setup/teardown and both current-meter rows pass;
2. both counts fall and effective compute does not regress materially;
3. current R75 versus R76 output drift stays negligible relative to the
   estimator error; and
4. local wall does not regress by more than one second on either row.

One receipt decides this spelling. No five-lane or broad rerun is warranted
unless it passes.
