# K129 R68 — exact view-batched R56/R27 wall repair

Evidence label before execution: **projection**.

## Objective and ceiling

Keep R66's R56 statistic and R27 arithmetic, but remove enough initialized
and steady transport work to make five concurrent remote workers credible
under the hard 60 s per-MLP wall limit.  The blocking screen is prediction
identity plus a five-lane initialized/steady maximum below the calibrated
safe band.  Count must remain below 145 B steady and 150 B initialized.

## Blocking prior-art search

Searches covered `Morton unstack`, `decoder unstack`, `affine view dedup`,
`cold row`, `initialized row`, and the exact owner/module names from the R66
census.  This found two relevant, previously measured mechanisms:

1. `candidate_k161_rightgram_d2right32_boundary_unstack_dedup...` interns
   structurally identical recipe descriptors before materialising public
   views.  It keeps the frozen add/subtract call sequence literal.
2. The K161 hierarchical decoder-unstack branch proves that one public
   unstack request may safely replace many scalar basic-index requests when
   the resulting view order is held in Python.

No prior source applies either mechanism to the R56 statistic on the R27
chassis.  No prior source batches the three persistent Morton right operands;
the current owner creates exactly 3,072 retained blocks with 3,072 separate
basic-index requests.

## Frozen spelling

- Port descriptor interning to both active R23 cached-plan classes, including
  the dual-order plan used by R26.
- Build each persistent Morton 32x32 block grid with reshape, transpose,
  reshape, and unstack; reorder the returned row-major Python tuple by the
  exact frozen Morton coordinates.
- Do not change an arithmetic operation, destination, order, sampler,
  coefficient, output scale, target access, or worker/row-dependent behavior.
- Restore every temporarily patched module symbol in `finally` blocks.

## Fast falsification

One exact packaged two-prediction official-runner gate is sufficient to kill
or retain the mechanism initially:

- both predictions must have the R66 hash
  `de892ae29a61e837d247ae408e72bc339e0c12cd367cac7a12d62f8846919014`;
- initialized and steady counted FLOPs must be stable and under the ceilings;
- bare setup and teardown must emit zero FlopScope traffic;
- the archive must pass the existing static/package audits.

Only a passing candidate proceeds to the calibrated five-lane screen.  No
target is opened and no remote action is authorized.
