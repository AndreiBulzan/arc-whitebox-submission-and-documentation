# K129 R77 — cold R75, steady R31 exact stack

Date: 2026-08-04

Evidence sought: **measured whole** for one initialized and one steady
target-free current-meter row; **component** for the inherited exact
contractions. No broad, upload, submission, or remote action is authorized.

## Prior-art boundary

R56 combined the R31 exact H1/late-B7 stack with mixed48/D6 and failed
remotely on the wall tail. R71/R75 subsequently added exact view-family
rewrites and a cold-row policy, but deliberately bypassed R31. No existing
artifact composes R31 only on R75's steady path while preserving the first
worker prediction as exact R27.

## Change and gate

Keep R75's first production prediction byte-for-byte on the R27 path. For
later predictions only, apply R30's standard-basis H1 DPS48 identity and
R31's batched late-B7 owner. Preserve R71/R68's view rewrites and the complete
mixed48/D6 statistic.

The v0.9 exact counted saving relative to R75/R27 is `2,166,727,680`. Promote
only if:

1. cold output/count stay exactly at the current-meter R75 values;
2. steady counted and effective compute improve by at least 1%;
3. steady output drift remains float32-reassociation scale; and
4. the isolated steady wall is no more than four seconds slower than R75.

Even a pass is not remote-ready: the prior R56 timeout requires a later
concurrent stress decision. One current-meter receipt first decides whether
that escalation is worth doing.

