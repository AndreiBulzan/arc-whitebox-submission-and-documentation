# Preregistration: matched-q0 independent96 test of literal 112/right17

Evidence sought: **broad statistical** comparison on 96 offline MLPs
(`384..479`) disjoint from the Full100/Generated128 selection banks. No
FlopScope row, package, upload, or submission is part of this test.

## Capsule prior-art preflight

Queries covered `112 primary`, `right 17`, `mixed orientation`, `sparse97`,
`rightgram`, `joint prune`, `independent96`, `holdout`, and the equivalent
operation “fixed total-K129 split between q0 and a rotated frame.” Nearest
controlling evidence:

- remote submission 322189 killed the aggressive sparse87/mixed42 spelling;
- the selection-disjoint sparse97/mixed32 holdout also failed to establish a
  robust transfer win;
- K141 rightgram12 and related rightgram screens add trajectories rather than
  holding total K fixed;
- `literal112_right17_r1_postseal_20260802.json` is the exact current rule and
  has reused-bank raw ratios `0.8981`/`0.8488`;
- `right112_17_independent96_r2_targetfree_20260802.npz` already seals the
  unchanged candidate predictions on rows `384..479`, but no matched standard
  K129 q0 prediction or score exists.

Outcome: **materially new observable**. This test does not tune the rule. It
only supplies the missing matched q0 control on the already captured rows.

## Fixed decision

The unchanged-calibration point threshold for `1.1e-7`, including the current
conservative two-arm overhead factor, is approximately raw ratio `0.900`.
Promote to exact deployment pricing only if:

1. pooled candidate/q0 raw-MSE ratio is `<=0.88` (transfer margin);
2. paired-bootstrap ratio p95 is `<=0.90`; and
3. at least 55% of rows improve.

Otherwise kill the exact 112/right17 rule as a reliable 1.1-class candidate.
