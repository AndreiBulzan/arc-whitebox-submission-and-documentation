# Multifidelity orientation R1 — hard kill

Evidence: **component** for the signed-premean screen; **projection** for the
remote arithmetic. No physical row, package, or remote action was performed.

The existing atlas cannot test the requested primary observable: it has
per-basis signed final preactivation means, but no per-basis or
per-orientation final post-ReLU means. The secondary signed screen is
decisively negative.

For `p=3`, `m=62`, total `K=148`, the two paid orientations are chosen
target-free as the pilot pair closest to the observed all-eight pilot
centroid. Pilot and high supports are disjoint, and the difference coefficient
is fixed at one.

| held family | current K146 signed MSE | difference MSE | ratio |
|---|---:|---:|---:|
| Full100 | `5.2903e-7` | `5.0007e-6` | `9.4525x` |
| Generated64 | `6.2382e-7` | `4.8874e-6` | `7.8347x` |

No held row improves. The fixed O0/O1 pair is worse still:
`20.92x` / `19.51x`. Disjoint within-orientation basis noise overwhelms the
finite-population correction, so no larger sweep is warranted.

The only positive capacity fact is not deployable: the literal mean of all
eight complete 129-basis orientations has signed MSE ratios `0.2216x` on
Full and `0.3335x` on Generated. It requires `1032` full-depth basis
equivalents and is therefore an oracle/capacity result, not a candidate for
the `272B` budget.

Artifacts:

- source SHA-256:
  `61d56ec0f2317202481178eaa27b0a35e0a8c3dec23edf56ff4113cde1b414eb`
- receipt SHA-256:
  `9c69ab31721bb46fdedbb25aeacc9ad5cc11dfe4141042a784b469f7b01a282d`

