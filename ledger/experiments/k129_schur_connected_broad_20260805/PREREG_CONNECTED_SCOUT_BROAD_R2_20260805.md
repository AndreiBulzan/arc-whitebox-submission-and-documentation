# Connected-scout broad confirmation R2

Date: 2026-08-05

The R1 mechanism is frozen unchanged: one 512-row antipodal q0 basis,
empirical per-layer gate fractions, squared-Jacobian pullback, gamma 0.50,
and the same one-plane-per-Schur-pair selector.  R1 failed its inappropriate
majority-row gate but changed only ten of 48 networks and reduced aggregate
raw MSE in all three families.  R2 tests the resulting sparse-tail hypothesis
without changing a parameter.

Capture the complete project Full100 and Generated128 banks and all 100 rows
of ARC's independent official Mini100 target-free.  The already sealed R1
positions 16--31 may be copied byte-for-byte; all other predictions are new.

The exact physical steady cost increase is `2.092892160B` counted FLOPs.  At
the current projected effective base this is a `1.0147632821` compute ratio.
Promote/upload only if:

- connected/R84 raw ratio is at most `0.985` independently on Full100,
  Generated128, and official Mini100;
- pooled Full+Generated raw ratio is at most `0.98`; and
- every family's projected adjusted ratio after the measured compute charge
  is below `1.0`.

No target-conditioned fallback or choice is allowed.  This is broad
statistical evidence, not remote evidence.

