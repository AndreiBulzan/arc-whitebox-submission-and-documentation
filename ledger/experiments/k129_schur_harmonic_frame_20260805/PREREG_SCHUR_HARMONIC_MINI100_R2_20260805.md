# Schur-harmonic complete-frame official Mini100 R2

Date: 2026-08-05

R1 was selected on fixed old Full4/Generated4 rows before this gate and is
sealed by post-seal receipt SHA-256
`71c0959c2f0d50bd03a6837721a7e46d176a8275fbf412b63d6e95bef8e3af52`.
No rule, ordering, or selector changes are allowed here.

Capture exactly the complementary angle-sorted Schur hybrids `angle_a` and
`angle_b` for all 100 rows of ARC's official Mini100.  The primary candidate
is the fixed `angle_b` rule.  The already frozen target-free moment selector
is secondary. Predictions and selector decisions are sealed before Mini100
targets are mapped.

Promotion requires the primary fixed rule to have raw-MSE ratio `<=0.92`
versus q0, at least 60/100 rows improved, and a paired bootstrap 95% ratio
upper endpoint below 1.0.  The selector is reported but cannot rescue a
primary failure. Passing licenses a current-meter frame implementation and
one physical row; it does not license packaging or submission.

