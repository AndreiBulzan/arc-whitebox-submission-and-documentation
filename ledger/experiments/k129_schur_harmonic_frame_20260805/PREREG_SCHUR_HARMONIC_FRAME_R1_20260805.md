# Schur-harmonic complete-frame compression R1

Date: 2026-08-05

Evidence target: target-free ordinary-CUDA component capture followed by a
fixed 4+4 post-seal falsifier. No FlopScope, package, Mini100, upload, or
submission action is licensed by this screen.

The broadly confirmed teacher is the equal output mean of the complete q0
and `polar(q0 + right + d2)` K129 frames.  Write the relative orthogonal
rotation as `P Q^T = Z T Z^T` in real Schur form.  Instead of the already
killed geodesic midpoint, R1 applies the polar endpoint on a balanced subset
of the invariant one/two-dimensional Schur blocks and the q0 endpoint on the
complement.  The resulting matrix is again one complete orthogonal frame.

Two target-blind partitions are frozen:

1. alternating blocks after sorting by rotation angle;
2. greedy balancing of first-layer energy over the Schur blocks.

Both complementary frames are captured.  A target-free selector chooses the
complement whose base-frame absolute/fourth-moment vector is closer to the
q0/polar midpoint.  This differs from prior row/basis mixing: no incomplete
basis is propagated, and every candidate remains exactly one orthogonal
frame.

Rows are fixed at Full `(200,203,206,209)` and Generated `(16,19,22,25)`.
Kill unless one *predeclared fixed or target-free-selected rule* has pooled
raw-MSE ratio at most `0.90` versus q0 in both families and improves at least
`5/8` rows overall. Passing licenses an unchanged official-Mini100 gate; it
does not license physical implementation.

