# Dual-frame basis-mixture shared suffix R1

Date: 2026-08-05

Evidence sought: target-free **component** capture followed by a fixed
Full4/Generated4 falsifier. Compute and adjusted values are **projections**
until a current-meter graph exists. No package, upload, submission, or remote
action is authorized.

## Prior-art boundary

The capsule already kills arithmetic cloud midpoints, Gaussian/mean suffix
handoffs, sparse independently repaired polar arms, static endpoint coresets,
and fitted frame selectors. It contains no spelling which propagates two
complete frames to a checkpoint and then retains one complete orthogonal
basis at every Kerdock index by choosing that whole basis from one of the two
clouds before an ordinary shared suffix.

This is not the killed midpoint. No particle values are averaged. It is also
not the sparse-polar sidecar: the propagated suffix always has exactly 129
complete antipodal bases and therefore keeps exact symmetry and per-basis
second-moment tightness.

## Fixed construction

For q0 and `polar(q0 + right + d2)`, propagate both full K129 clouds through
checkpoint `l`. Their rows have the same `(sign, basis, direction)` labels.
Form one K129 cloud by taking:

```text
even zero-based basis indices  <- q0
odd zero-based basis indices   <- polar
```

Both positive and negative halves of a selected basis come from the same arm.
Then run one ordinary nonlinear suffix and the unchanged K129 readout.

Fixed checkpoints, post-ReLU and zero-based: `{4, 8, 12, 16}`. Pilot rows are
Full `{7,17,27,37}` and Generated `{0,1,2,3}`. Lambda is `0.0075`; output
scale is `1.000025`.

The conservative economic model inherited from the midpoint falsifier is

```text
C_l = 273.5B - 4.0B * (31-l) + 1.0B.
```

After the target-free archive is sealed, select the checkpoint minimizing the
worse Full/Generated projected adjusted score. Promote one fixed checkpoint
to official Mini100 only if both families project to at most `1.10e-7`, have
raw ratio to q0 at most `0.82`, and improve at least 3/4 rows.

