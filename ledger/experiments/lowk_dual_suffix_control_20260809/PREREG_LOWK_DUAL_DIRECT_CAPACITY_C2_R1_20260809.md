# Low-K dual direct-support capacity C2 R1

Date: 2026-08-09

Evidence sought: target-free CUDA **component** capture on the fixed sixteen
public development rows, followed by a post-seal direct-support capacity
diagnostic.  No Mini100, physical, package, upload, or remote action.

The C1 nonlinear basis-mean residual control is killed.  It does not determine
whether the independently reported layer-5 direct coreset is selecting useful
complete tails, because C1 imposed a coarse-derived shared late-width support
that worsened the forty-tail control.

C2 therefore captures the literal per-basis endpoints of the already-frozen
K20 ordinary and K20 polar low-K trajectories.  Each arm uses its own actual
late energy supports and its existing endpoint correction.  The mean of the
twenty per-basis endpoints must reproduce the previously sealed arm prediction.
The already-sealed C1 layer-5 herding order is then applied without change.

Post-seal diagnostics at `8,12,14,16,20` total tails:

1. equal-weight direct checkpoint-herding support;
2. fixed-prefix support;
3. target-aware nonnegative simplex/ridge weights on the frozen support;
4. target-aware greedy support followed by stable nonnegative weights.

The last two are capacity oracles only.  They answer whether support selection
or weight estimation is the missing statistic; they are not deployable.

Proceed to a smarter target-free selector only if a `<=14`-tail stable oracle
reaches raw MSE `<=1.0e-6`, preferably `<=8.0e-7`, while the frozen target-free
herding rule shows at least the correct direction.  If even the stable
target-aware oracle misses `1.0e-6`, close direct whole-basis tails at this
floor geometry.  The independently reported friend's exact algorithm remains
external until its files and official Mini100 coreset receipt are available.

