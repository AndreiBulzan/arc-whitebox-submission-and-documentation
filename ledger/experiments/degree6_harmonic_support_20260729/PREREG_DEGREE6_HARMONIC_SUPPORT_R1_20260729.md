# Degree-6 harmonic support R1 preregistration

Date: 2026-07-29

Evidence sought: **component**.  This is a compact-geometry support scout
followed by an existing-atlas signed-preactivation falsifier.  It authorizes
no estimator propagation, FlopScope session, physical/timed row, package,
upload, submission, or remote action.

## Hypothesis

The complete orientation-0 Kerdock arm is an exact spherical 5-design.  Its
first possible angular integration defect is therefore degree 6.  For a
Kerdock basis `B`, define its sixth-moment tensor

```text
A_B = mean_{b in B} b**tensor(6).
```

For two bases,

```text
<A_B, A_C> = mean_{b in B,c in C} (b dot c)^6.
```

Every orientation-1 basis has the same self term and every distinct pair
inside orientation 1 is mutually unbiased, so those contributions are
constant for a fixed support size.  Consequently the exact equal-weight
support minimizing the combined sixth-moment defect of:

```text
all 129 orientation-0 bases + m orientation-1 bases
```

is obtained by ranking each orientation-1 basis `j` by:

```text
score[j] = sum_{i=0}^{128} <A_O0[i], A_O1[j]>.
```

The fixed runtime excludes the orientation-1 zero basis, so R1 ranks only
bases `1..128`.  The nested first 17 and first 33 bases are frozen using
only the compact Kerdock rotations and phase table.  No network weights,
endpoints, predictions, or targets may be opened by the support-freeze
phase.

## Held falsifier

After the support NPZ and receipt are written and hashed, a separate scorer
opens the already-frozen eight-orientation endpoint atlas.  It evaluates the
same Full100 and Generated64 row indices used by the sealed K146 broad
capture.

For `m=17` and `m=33`, compare the literal signed endpoint:

```text
(129 * mean(O0 all129) + m * mean(O1 frozen_support_m)) / (129 + m)
```

against the current K146/m17 support on exact signed-final-preactivation
targets.  No coefficient, reweighting, fit, size selection, retry, or
family-specific choice is permitted.

## Hard gate

Promote a support size only if it independently satisfies on both Full100
and Generated64:

```text
pooled signed endpoint MSE / current K146 MSE <= 0.50
row-ratio p95                               <= 1.20
all arrays finite
```

Failure of both fixed sizes closes this exact degree-6 support spelling.
Passing licenses only a target-free full-prediction capture of the fixed
support; it is not itself a final-ReLU candidate receipt.

## Economics

The support replaces basis identities inside an otherwise identical graph.
Prospective runtime operation delta is exactly **0B**.  K146/m17 and
K162/m33 retain their existing total-operation envelopes.  The offline
geometry calculation and atlas scoring are not runtime operations.
