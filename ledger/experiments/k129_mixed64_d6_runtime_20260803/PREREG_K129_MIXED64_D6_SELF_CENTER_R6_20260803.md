# K129 mixed64/D6 self-centered sidecar — R6 preregistration

Date: 2026-08-03

## Question

The sealed mixed64 statistic centers each alternate depth-6 basis proxy on
the complete q0 depth-6 basis mean.  The production K129 chassis does not
materialize that full-width q0 depth-6 state: its paid layer-6 boundary is a
232-wide compressed state.  Can the alternate arm center on its own 64-basis
mean and retain the transferable correction, thereby deleting the entire q0
capture and continuation sidecar?

## Blocking prior-art preflight

Searches covered `self-center`, `within-arm`, `alternate mean`, `proxy
center`, and `centering`.  Existing work centers regression columns, tests
within-arm uncertainty, or substitutes the paid q0 endpoint.  No prior result
uses the mean of the independently repaired alternate D6 proxies as the
reference for this mixed64 teacher correction.  The paid-q0 endpoint failure
does not answer this question because that endpoint is neither the alternate
mean nor the unavailable full-width q0 D6 mean.

## Frozen target-free construction

- Inputs: the already-sealed Full100/Generated128 literal mixed64 D6 proxy
  bank and complete four-frame target-free teacher.
- Feature: `alternate_i - mean_i(alternate_i)` for each of the 64 frozen
  slots; frames, bases, 256 directions, and literal repairs are unchanged.
- Fit: the existing four-fold family-balanced ridge-1 solve, then one all-row
  fit.  No challenge target participates in fitting or selection.
- Seal predictions before opening targets.

## Gates

Promote only if post-seal raw MSE improves over q0 on both complete families,
with Full100 ratio at most `0.82` and Generated128 ratio at most `0.86`.
Those thresholds leave a plausible route to `1.1e-7` after the exact physical
sidecar price is known.  Failure kills this spelling without scalar, ridge,
support, or family retuning.

