# K129 mixed64/D5 post-snap checkpoint preregistration

Date: 2026-08-03

Evidence target: **broad statistical** for sealed accuracy; **projection** for
compute until a physical descendant exists.

## Prior-art preflight

Capsule searches covered `D5`, `depth 5`, `start_layer=5`, `D4`, `D6`,
`multidepth`, and the literal mixed64 lineage.  Existing screens use D4/D6
or depths 6/8/10/etc.; no literal mixed64 D5 candidate or verdict exists.
D5 is structurally distinct from killed D4: it retains exact layer 4 and the
existing L4 moment snap, then starts diagonal continuation at layer 5.

## Fixed candidate

- Keep all 64 frozen slots, all 256 directions, ridge 1.0, and the q0 D6
  anchor.
- Run the alternate cloud exactly through layers 2, 3, and 4, including the
  frozen L4 snap.
- Take per-basis moments after layer 4 and begin the existing diagonal
  continuation at layer 5.
- Refit only the 64 teacher coefficients by the existing target-free,
  family-balanced four-fold procedure.
- Seal Full100 and Generated128 predictions before opening targets.

## Promotion rule

Price the deletion conservatively as one sixth of the old six-stage literal
prefix price.  Promote if the controlling adjusted projection improves on
R34's `1.124471608e-7`; strong promotion requires `<=1.10e-7`.  Otherwise
kill before any FlopScope implementation.

