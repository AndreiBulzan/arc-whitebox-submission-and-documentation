# K129 signed-mean student R1 preregistration

Date: 2026-08-01

Evidence: **component development diagnostic** only.  This is a cache-only
post-seal screen over an existing target-free sparse-pilot capture.  It
authorizes no estimator build, physical row, package, upload, submission, or
remote claim.

The sealed K129 capture contains 129 per-basis first moments of the final
preactivation.  Cross-fit a shared, output-permutation-equivariant ridge map
from their normalized basis pattern to the high-sample signed-mean residual.
Apply the result to the current readout only through the exact identity

```text
E[ReLU(z31)] = 0.5 E[z31] + 0.5 E[|z31|].
```

No absolute-value correction is learned.  Promote to a broad target-free
capture only if the 16-row diagnostic achieves signed-mean ratio `<= 0.80`,
final-output ratio `<= 0.92`, and improves at least 10 of 16 MLPs.
