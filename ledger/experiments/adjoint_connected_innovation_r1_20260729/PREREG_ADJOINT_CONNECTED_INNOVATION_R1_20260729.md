# Adjoint connected-innovation recurrence R1

Date: 2026-07-29

Evidence sought: a target-free offline capture followed by one sealed
**component** score on two already-used Full and two already-used Generated
development rows.  No FlopScope session, physical row, package, upload,
submission, or remote action is authorized.

## New observable

R1 tests a first-order connected-cumulant transport state, not another
one-site cavity, marginal K3/K4 recurrence, endpoint repeated-view PTCC
readout, or learned Stage-A model.

First run the fixed order-eight Gaussian/Price mean-covariance closure.  Let
`D_l = diag(Phi(pre_mean_l / pre_sigma_l))` be its ReLU Jacobian.  Backward
mean response from postactivation layer `l` to the final postactivation is

```text
J_31 = I
J_l  = W_(l+1) D_(l+1) J_(l+1),  l = 30,...,0.
```

`P_l` is the leading 16-dimensional left-singular subspace of `J_l`.
Only the subspace matters: every core operation is covariant under
`P_l -> P_l Q`, so SVD signs and rotations are gauge choices.

In that basis carry symmetric connected cores

```text
T3_l[a,b,c]       ~= Cum((h_l-m_l)P_l)_abc
T4_l[a,b,c,d]     ~= Cum((h_l-m_l)P_l)_abcd.
```

The inherited state crosses a realised weight and ReLU through

```text
B_l = P_(l-1)^T W_l D_l P_l
Tn_l(inherited) = Tn_(l-1) x_1 B_l ... x_n B_l.
```

This transports sign-bearing three- and four-neuron interactions rather
than retaining only marginal or repeated-index queries.

## Local connected birth

At every layer `0..30`, use one fixed 512-node antithetic, covariance-exact
Gaussian cubature.  The standard-normal node matrix is generated once from
256 scrambled Sobol points plus their negatives and right-whitened so its
sample mean is zero and covariance is exactly identity.

For the Price Gaussian `Z_l`, evaluate

```text
q     = (ReLU(Z_l) - sample_mean(ReLU(Z_l))) P_l
q_lin = ((Z_l-pre_mean_l) D_l) P_l.
```

The local innovation is

```text
birth_n = Cum_n(q) - Cum_n(q_lin),  n in {3,4}.
```

The common-random-number subtraction makes every birth exactly zero for an
identity activation under the finite cubature.  It therefore removes the
cubature's spurious Gaussian cumulant and isolates the genuinely nonlinear
multi-site connected birth.  Set

```text
Tn_l = Tn_l(inherited) + birth_n.
```

This differs from the killed leading-tree closure: it retains the complete
rank-16 projected connected core generated by the nonlinear map, including
loops and all-distinct terms, and transports that MLP-specific core through
subsequent realised weights.

## Mean readout

Before ReLU `l>0`, project the previous postactivation core through the
actual columns of `W_l`:

```text
A = P_(l-1)^T W_l
kappa3[o] = T3_(l-1)(A_o,A_o,A_o)
kappa4[o] = T4_(l-1)(A_o,A_o,A_o,A_o).
```

Use the coefficient-one first-order Edgeworth response

```text
e = -kappa3 * alpha * phi(alpha) / (6 var)
    +kappa4 * phi(alpha) * (alpha^2-1) / (24 var^(3/2)).
```

The accumulated mean innovation is

```text
delta_post_l = D_l (delta_post_(l-1) W_l) + e_l,
delta_post_0 = 0.
prediction_l = Price_mean_l + delta_post_l.
```

Each layer is clipped only to the parameter-free physical interval
`max(0, pre_mean+delta_pre) <= prediction <=
sqrt(pre_var+(pre_mean+delta_pre)^2)`.  There is no fitted coefficient,
target statistic, learned parameter, endpoint blend, or row routing.

## Frozen protocol

```text
rank                         16
cubature nodes              512
Price order                   8
connected-birth layers     0..30
Full rows                  0, 1
Generated rows             0, 1
```

Capture opens only `weights.npy` from the two existing archives.  It must
freeze the literal Price baseline and R1 predictions, indices, diagnostics,
source hash, prediction hashes, and archive hash before a separate scorer
may open `targets.npy` / `target.npy`.

## Gates and stop

The recurrence is mathematically coherent only if:

1. whitened node mean absolute maximum is `<=2e-12` and covariance maximum
   absolute error is `<=2e-10`;
2. the identity-activation birth unit check is `<=2e-10` absolute;
3. every core, prediction, and bound is finite;
4. the static current-meter arithmetic projection is `<=20B` added work.

After sealing, continue this class only if the R1 final raw MSE is
`<=0.10 * Price8` separately on Full2 and Generated2, at least one row
improves in each family, and no row exceeds `2x` its Price8 loss.  The
`0.10` gate is intentionally weak in absolute terms: Price8 is around
`6e-5`, while a multiplier-floor route still needs raw error around
`1e-6`.  Failure kills this exact rank-16 projected connected-innovation
recurrence without a rank, node, layer-window, coefficient, clipping, or
quadrature sweep.

## Cost projection boundary

Using the released dense formulas (`matmul m*n*(2k-1)`,
`eigh/svd/cholesky n^3`) gives the following conservative ordinary-operation
envelope:

```text
Price full-covariance closure                       2.2 B
backward response products + 31 SVDs                2.1 B
31 covariance square roots                           0.6 B
512-node covariance maps over 31 births              2.1 B
node projections and moment cores                    3.0 B
core transports, directed queries, elementwise       4.0 B
layout/movement/safety allowance                     4.0 B
----------------------------------------------------------
projected total increment / standalone graph       18.0 B
```

This is a **projection**, not a FlopScope receipt.  If the accuracy gate
passes, the next action would be an exact operation-graph audit, not a
physical run.
