# Response-projected production-trajectory R1 — fast capacity gate

Date: 2026-07-29

Evidence sought: a target-free **component** feature seal followed by one
split-sealed **broad statistical development diagnostic**.  This is not a
FlopScope run, measured whole, candidate, package, upload, submission, or
remote action.

## New observable

The previous matched-orientation lane tried to reconstruct the omitted O1
population and found that L4/L8 features added nothing beyond the endpoint.
It did not ask the target-aligned question below and did not transport the
intermediate discrepancies through the realized downstream weights.

For the paid K146 geometry, use the complete O0 arm and fixed S17 O1 arm.
At each of L4, L8, and the signed W31 endpoint form:

```text
d_l = mean(O1 S17) - mean(O0 all129)
x_l = mean(O0 S17) - mean(O0 all129)
```

For L4/L8, right-project `d_l` and `x_l` through the already captured
target-blind analytic downstream response matrix `R_l`.  The six fixed
width-256 features are:

```text
d_final, x_final, d_L4 R_L4, x_L4 R_L4, d_L8 R_L8, x_L8 R_L8.
```

The signed baseline is the literal support blend

```text
b = (129*mean(O0 all129) + 17*mean(O1 S17)) / 146.
```

One no-intercept ordinary least-squares map with six shared scalar
coefficients predicts `target_final_premean - b`.  Coefficients are shared
over every MLP and output coordinate.  There is no neuron identity, row
identity, seed, family flag, intercept, nonlinear model, coefficient grid,
feature selection, or held-family tuning.

This observable is distinct from:

- the killed endpoint-matched R5, whose label was O1 S109 rather than the
  exact signed network mean;
- the killed matched R6 kernel, which did not use a downstream response or
  exact signed residual;
- the killed K32 aggregate recurrence, which did not retain the production
  two-arm/per-basis state; and
- the killed 26-particle K178 gate-innovation accumulator, whose controlling
  failure was pilot noise.

## Fixed data and split

Phase A may open only the named, already frozen K258 production-aligned
atlas members:

```text
indices.npy
completion.npy
observer_basis_postmean.npy
final_basis_premean.npy
response_matrix.npy
```

It uses only orientations 0/1 and checkpoints L4/L8.  It must serialize
features and baselines for Full200 and Generated128 before any exact signed
target is opened.

Phase B verifies the Phase-A hashes, then:

- fits the six coefficients on the frozen `full_train_indices` 100-row
  support-search split;
- evaluates only on its Full100 complement and the frozen Generated64
  complement; and
- never refits, rescales, or selects on either evaluation family.

The atlas `target_final_premean.npy` members are forbidden to Phase A and
opened only by Phase B after the feature seal.

## Fixed hard gate

Advance to a tiny actual-production K146/K162 trajectory capture only if
both held families satisfy every condition:

```text
candidate / baseline signed MSE        <= 0.75
row-ratio p95                          <= 1.20
rows improved                          >= 0.60
all coefficients and predictions finite
```

The threshold is deliberately material: a weak proxy win does not justify a
new production capture.  Failure kills this six-feature response-projected
linear spelling.  Do not scan checkpoints, coefficients, ridge strengths,
feature subsets, nonlinearities, or response definitions after failure.

## Deployment economics boundary

If the gate passes, the production implementation needs only:

- three arm/subsupport reductions at L4, L8, and the W31 endpoint;
- one analytic downstream response recurrence, retaining the L8 and L4
  snapshots; and
- six scalar-vector products plus five vector adds.

The existing L4 response audit prices one shared L4/L8-compatible backward
response recurrence below `0.873B` ordinary operations.  The reductions and
final combination add less than `0.1B` on K146.  Thus the intended increment
is below `1.0B` counted operations before movement/call/residual pricing.
This is a **projection**, not a receipt.

Passing remains only a license for a fixed 4-Full/4-Generated actual
production-state capture.  It does not authorize physical execution,
packaging, or remote action.
