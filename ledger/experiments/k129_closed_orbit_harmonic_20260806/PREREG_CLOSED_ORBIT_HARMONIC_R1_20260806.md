# Closed-orbit complete-frame harmonic scout R1

Date: 2026-08-06

Evidence sought: a target-free ordinary-CUDA **component** capture on fixed
Full4 and Generated4 rows, followed by a separate post-seal component score.
No FlopScope session, estimator edit, package, upload, submission, or remote
action is authorized.

## Blocking prior-art audit

The capsule was searched for `TAP`, `Onsager`, `cavity`, `AMP`, `harmonic`,
`Fourier`, `Gegenbauer`, `degree-6`, `geodesic`, `rotation`, `zonoid`,
`great-circle`, `cubature`, and the complete-frame production call sites.

Controlling negatives:

- `tap_cavity_finitewidth_20260729` and `one_site_cavity_closure_20260729`
  explicitly implement the relevant cavity family.  Ordinary Onsager
  self-reaction is absent in this feed-forward DAG, and the stronger lawful
  one-site/query-conditioned variants miss their gates decisively.  That
  lane is not reopened.
- `coupled_harmonic_residual_cloud_20260729` kills the first spherical
  harmonic carried through depth.
- `degree6_harmonic_support_20260729`,
  `degree6_adjoint_support_20260729`, and
  `polarized_degree6_control_20260729` test static and proxy-routed degree-6
  support/control rules; they improve too little or reverse by family.
- `higher_design_quadrature_20260729` proves that a weight-independent exact
  spherical design of strength above five cannot fit the node budget.
- `harmonic_alias_decoder_20260729` kills fixed affine decoding of the paid
  basis endpoints.
- the polar/geodesic and Schur-harmonic work samples isolated means or
  partitions relative-rotation planes using assumed Fourier features.  It
  never measures the actual complete-K129 estimator around a closed
  rotation orbit.
- `exact_circle_partition_20260729` integrates individual great circles
  exactly but is killed by between-plane coverage and compute.  R1 below is
  not an exact piecewise-cell integrator.

Prior-art outcome: **materially new observable**.  R1 directly measures the
Fourier spectrum of the full nonlinear K129 prediction as the entire exact
5-design moves around one target-free, weight-conditioned, closed subgroup
of the orthogonal group.  It does not infer that spectrum from weights,
moments, a linear adjoint, or basis identifiers.

## Construction

For each network, construct the already defined stable inputs:

```text
Q0 = canonical complete-frame orientation
P  = polar(Q0 + Qright + Qdepth2)
R  = P Q0^T = Z T Z^T                 (real Schur form)
```

Use the invariant two-planes in `Z` only to define a complex structure.
Every native two-plane rotates with unit angular speed and the sign of its
Schur block; real one-dimensional blocks are paired deterministically.  The
closed orbit is

```text
Q(theta) = Z block_rotation(theta) Z^T Q0,
theta = 2 pi j / 16, j=0..15.
```

Every `Q(theta)` is one complete orthogonal K129 frame, so every individual
arm retains the exact degree-five design identities.  The target-free
capture stores the 16 literal nonlinear final predictions, their Fourier
power by mode, and equally spaced 2/4/8/16-angle means.

## Fixed scout rows and gates

```text
Full       400, 403, 406, 409
Generated  112, 115, 118, 121
```

Targets remain unopened until the capture and hashes are sealed.  Continue
to a larger disjoint bank only if all of the following hold:

1. the 16-angle mean has raw-MSE ratio at most `0.65` to theta-zero in each
   family;
2. the four-angle mean has ratio at most `0.80` in each family;
3. the 16-angle mean improves at least `6/8` rows overall; and
4. actual modes 1--6 carry at least `75%` of non-constant orbit power.

These are capacity gates, not deployment economics.  Sixteen or four full
frames cannot fit the production budget.  A pass licenses exactly one next
question: can a phase-interlaced total-K129 support reproduce the circle
mean while restoring the design identities?  A failure kills this closed
Schur-orbit spelling without an angle-count, row, or phase sweep.

## Target ceiling

Remote R87 is `1.14333e-7`; crossing `1.0e-7` at unchanged price needs about
`12.54%` raw reduction.  A useful same-budget descendant therefore needs a
raw ratio no worse than about `0.875`.  The scout demands much stronger
teacher ratios because the later one-budget compression will retain only a
fraction of the teacher gain.  If the full orbit cannot reach `0.65` on both
families, it has no credible ceiling for that compression step.

