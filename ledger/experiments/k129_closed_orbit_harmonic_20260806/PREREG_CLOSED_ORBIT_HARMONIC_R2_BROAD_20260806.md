# Closed-orbit complete-frame harmonic R2 disjoint expansion

Date: 2026-08-06

R1 was frozen before targets and passed its fixed Full4/Generated4 component
gate on every row.  R2 changes no frame construction, angle, coefficient, or
aggregation rule.  It expands the literal target-free capture to disjoint
fixed rows:

```text
Full       416..431
Generated   96..111
```

Evidence sought: target-free ordinary-CUDA **component** capture followed by
a separate post-seal **broad statistical** score.  No FlopScope session,
estimator edit, Mini100 access, package, upload, submission, or remote action
is authorized.

The prior-art boundary is inherited from R1.  The new observable remains the
actual full-K129 nonlinear output spectrum around the closed, Schur-aligned
rotation orbit; no earlier harmonic proxy or Schur partition measured it.

Promote only if all fixed conditions hold:

- 16-angle mean raw ratio to theta-zero `<=0.55` in both families;
- 8-angle mean ratio `<=0.60` in both families;
- 4-angle mean ratio `<=0.75` in both families;
- the 4-angle mean improves at least `24/32` rows; and
- modes 1--6 retain at least `75%` of nonconstant orbit power.

A pass is still teacher/capacity evidence.  It licenses one same-budget
construction: distribute the 129 complete orthonormal bases across orbit
phases while explicitly minimizing the aggregate fourth-moment defect.
Because each antipodal orthonormal basis already fixes moments through degree
three, the only exact-design identity that mixing can break before degree six
is the fourth moment.  That is the mathematical constraint the next step must
restore; arbitrary Latin allocation remains closed.

