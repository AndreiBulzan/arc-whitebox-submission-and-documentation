# K162 late-184 fast falsifier

Date: 2026-07-29

## Question

Does changing only the K162/m33 late energy keep from `192` to `184`,
while retaining final keep `176`, improve adjusted-score economics on both
development families?

The physical M248 tail admits keeps divisible by eight.  Existing exact
static arithmetic gives:

```text
count saving = 8 * 162,077,370 = 1,296,618,960
```

No physical implementation is authorized by this experiment.

## Protocol

1. Capture only weights-derived `q0` and `q1` predictions for the frozen
   Full100 and Generated64 row sets, using the frozen S33 support and the
   existing CUDA replay with late keep `184` / final keep `176`.
2. Seal the archive and its source hash before opening targets.
3. In a separate score stage, compare the literal `129:33` blend with the
   existing sealed late-192/final-176 K162 prediction bank.
4. Report direct raw ratios and residual-held economics at the measured
   persistent-operands R1 price.

## Fixed gates

The candidate advances only if:

- raw-MSE ratio is `< 1` on both Full100 and Generated64 corrected;
- projected adjusted central under the conservative R1 calibration is
  `<= 1.2e-7` on both families; and
- every prediction is finite.

This is a broad statistical development falsifier, not confirmation,
measured-whole, package, or remote evidence.

