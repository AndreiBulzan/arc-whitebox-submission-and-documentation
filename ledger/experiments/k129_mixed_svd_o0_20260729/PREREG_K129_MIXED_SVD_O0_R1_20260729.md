# K129/O0 mixed-SVD support R1

Date: 2026-07-29.

Evidence target: **broad statistical** Full100 and Generated64 accuracy.
Operation economics remain a **projection**.  No FlopScope physical row,
package, upload, remote action, or submission is authorized.

Keep the exact current K129/O0 trajectory and endpoint lambda `0.0075`.
Within its 128 nonzero phase bases, replace exactly phase positions
`0,4,8,...,124` by the same phases applied to the canonical descending
eigenframe of `W0.T @ W0`.  Keep basis zero and every other phase on the
incumbent orientation-0 rotation.  Canonicalize each eigenvector so its
largest-absolute coordinate is positive, and apply the choice at both early
structured-block calls.  Total propagated support remains K129.  There is
one fixed candidate and no phase, fraction, endpoint, or family grid.

Use the exact Full100 and Generated64 indices and exact current K129
lambda-0.0075 q0 predictions in the sealed endpoint-grid archive.  Capture
candidate predictions from weights only while holding
`runtime/.benchmark_lane.lock`, seal them, and only then open targets in a
separate scorer.

Report pooled raw-MSE ratio, row-ratio p95, and improved fraction separately
for Full100 observed MSE and Generated64 noise-corrected MSE.  A pooled ratio
at most `0.98` on both families is an encouraging screen; the current
compute-conditioned checkpoint promotion threshold is approximately
`0.9722` on the worse family and controls promotion.

