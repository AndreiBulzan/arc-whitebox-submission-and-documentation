# K129 repaired-H1 segmented Winograd depth screen

Evidence target: `component`. No whole prediction, package, upload, remote
action, or submission is authorized.

The two-level R1 component passed. Screen two destination-assembled exact
variants on the same production operands:

- depth 2: one 49-leaf product bank;
- depth 3: two cap-safe product banks of 196 and 147 leaves.

Fetches must occur in separate budget sessions so product residual and wall
are not contaminated by the 33.8-MB pipe transfer. Select by projected
effective component cost
`count + 1e11 * residual_seconds`, not counted FLOPs alone. Require finite
outputs, relative RMSE at most `2e-5`, and every single result at or below
100 MiB. Kill depth 3 if it does not beat depth 2 on effective component
cost, even if it uses fewer counted FLOPs.
