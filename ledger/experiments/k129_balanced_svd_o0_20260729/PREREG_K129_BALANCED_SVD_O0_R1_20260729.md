# K129/O0 balanced-SVD support R1

Date: 2026-07-29.

Evidence target: **broad statistical** Full100 and Generated64 accuracy.
Operation economics remain a **projection**.  No FlopScope physical row,
package, upload, remote action, or submission is authorized.

This candidate was fixed independently before the ordinary right-eigenframe
O0 result was opened.  Keep the exact current K129/O0 trajectory, endpoint
lambda `0.0075`, basis zero, and every propagation/readout choice.  Within
the 128 O0 phase-array entries, replace exactly positions
`1,5,9,...,125`.  This excludes the sole non-bent stored phase at position
zero; the flat-energy construction uses the fixed `p = 1 (mod 4)` class.

For `W0 = U S V.T`, construct `U` from the descending eigendecomposition of
`W0 @ W0.T`, canonicalizing each eigenvector so its largest-absolute
coordinate is positive.  The replacement frame is

`F = H @ U.T`,

where `H` is the exact normalized order-256 Sylvester Walsh-Hadamard matrix
with entries `+/- 1/16`.  Apply the same fixed phase choice at both early
structured-block calls.  This preserves orthogonality while equalizing the
row energies of `F @ W0` in exact arithmetic.  Require float32-frame
orthogonality error and relative transformed-row-energy spread each below
`2e-6`.

There is one candidate only: no phase, fraction, endpoint, sign, or family
grid.  This exact class was fixed before capture or scoring.  Use the exact
sealed Full100/Generated64 indices and K129
lambda-0.0075 baselines.  Capture from weights only under
`runtime/.benchmark_lane.lock`, seal predictions, then score in a separate
target-opening process.
