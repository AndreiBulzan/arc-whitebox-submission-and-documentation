# K129 R79 single complete polar frame — physical promotion preregistration

Date: 2026-08-04

The numerical mechanism already has **broad statistical** official-Mini100
evidence: `polar(q0 + right + d2)` scored raw ratio `0.925818` to q0.  R79
places that one complete frame into the exact R78/R31 execution chassis.  It
does not use the killed sparse arm or the two-trajectory output blend.

## Frozen construction

Inside the prediction budget, using only `flopscope.numpy`:

1. construct canonical descending float32 eigenframes of `W0.T W0` and the
   fixed depth-2 Gaussian-gated pullback `J2 J2.T`;
2. cast q0/right/d2 frames to float64 and form their equal mean;
3. take its full float64 polar factor `U @ Vh`, cast once to float32; and
4. substitute that frame for R31's O0 rotation while leaving every K129
   propagation, repair, readout, and R78 descriptor-interning operation
   unchanged.

The implementation must restore the setup-owned q0 frame after every call.
No participant-side NumPy or unmetered arithmetic is permitted.

## One-run gate

Run initialized and steady Full row 17 through the pinned FlopScope 0.10.0 /
WhestBench 0.14.0 LocalRunner.  Promote only if:

- both predictions are bit-identical;
- final-row RMSE to the already sealed ordinary-CUDA polar Full17 prediction
  is at most `2e-5` and maximum absolute error at most `1e-4`;
- steady counted work is at most `128B`, i.e. the frame side cost is under
  `3.661B` versus R78/R31's `124.339494082B` ledger;
- both rows are below the `272B` hard cap and local wall below `55s`; and
- bare setup/teardown emit no FlopScope requests.

For the remote-score projection, the stricter controlling condition is that
the frame's total effective delta over R78 is at most `4.715B`.  At that
boundary the Mini100 central calibration is `1.10e-7`.  This remains a
**projection** until remote grading.

No package, upload, submission, or remote action is authorized.

